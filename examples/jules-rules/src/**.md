## src/ rules

- Any usage of `eval`, `Function(...)`, or `child_process.exec` with user input is BLOCKING.
- Every exported function must have a JSDoc type comment.
- Direct database writes outside `src/repositories/` are BLOCKING.
